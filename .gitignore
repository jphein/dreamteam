# Runtime state — regenerated by hooks each session (roster, footprint log,
# active marker, handoff). Only the placeholder keeps the dir in git.
state/*
!state/.gitkeep

# Editor / OS cruft
*.tmp
*.swp
.DS_Store
