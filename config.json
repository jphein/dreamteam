{
  "_comment": "dreamteam tunables. All hooks/scripts read this via jq with the same defaults baked in, so it works even if this file is absent. Calibrated to the VERIFIED 2026-06-30 OOM postmortem on katana (32 GB RAM / 32 GB swap): 59 distinct Claude procs at OOM time, TWO simultaneous balloons (3.7 GB + 3.4 GB), 17.2 GB resident; baseline ~200-280 MB/agent.",
  "memory": {
    "maxAgents": 36,
    "perAgentMB": 300,
    "hostReserveMB": 6000,
    "balloonReserveMB": 8000,
    "minAvailableMB": 8000,
    "scopeToDreamteam": false,
    "_notes": {
      "maxAgents": "SYSTEM-WIDE cap (mem-gate counts ALL claude procs via pgrep, not just this team). 59 ran on 06-30 across multiple sessions \u2014 a per-team cap can't see the aggregate; this can. Raised 30\u219236 on 2026-07-01 after the cap (not memory) became the binding constraint during a real multi-fleet day (31 procs, budget said 7 more agents fit); the memory budget + 8G floor remain the true guards, and 36 still sits well under the 59-proc disaster load.",
      "perAgentMB": "Planning RSS for a NEW agent. Verified baseline at OOM time was ~200-280 MB; live-active with context ~350-430 MB. Set to 300 (2026-07-01) \u2014 the measured baseline, not the padded active figure, because the balloonReserve + earlyoom + cgroup handle spikes and the minAvailableMB floor is the real backstop. This is a THROTTLE (how fast we approach the floor), not a safety limit. (Earlier 600 was a cgroup-peak-incl-cache artifact; 400 over-padded.)",
      "balloonReserveMB": "Headroom for runaway growth of ALREADY-ADMITTED agents \u2014 including their CHILD toolchains (gradle/JVM daemons on candela days), which the claude-proc accounting cannot see. Cut 8->4G on 2026-07-01 morning; RESTORED to 8G the same day after the 16:06 systemd-oomd kill: fleets admitted under the 4G reserve grew ~10GB of build-tool memory and oomd killed the shared Ghostty scope at 70% mem+swap. Admission math is spawn-time-only; this reserve is what absorbs post-admission growth. Loosen only inside a launch-dreamteam.sh MemoryMax scope.",
      "scopeToDreamteam": "false = GLOBAL safety net on every agent spawn (recommended \u2014 06-30 spanned multiple sessions). true = only acts when a team marker is active (state/active)."
    }
  },
  "reuse": {
    "enforce": true,
    "_notes": {
      "enforce": "true = reuse-gate BLOCKS a fresh spawn when an idle teammate can take the task (reuse via SendMessage = zero new RAM + warm context). Override per-spawn with 'FRESH-SPAWN: <reason>' in the prompt. false = disable the gate entirely."
    }
  },
  "dashboard": {
    "port": 8383,
    "host": "0.0.0.0",
    "_notes": "Local mission-control dashboard service (issue #11). scripts/dashboard-server.py serves templates/dashboard.html + /api/data (dashboard-data.sh --json --all-teams, 5s cache) + /api/version. host defaults to 127.0.0.1 (localhost only, safe); set \"0.0.0.0\" to expose on the LAN (homelab norm \u2014 opt-in). Manage via scripts/dashboard-ctl.sh {install|start|stop|status|logs} \u2014 a systemctl --user unit (systemd/dreamteam-dashboard.service, MemoryMax 256M). Readers default to 8383/127.0.0.1 if this section is absent, per the file's baked-defaults idiom; env DREAMTEAM_DASHBOARD_PORT/HOST override at runtime."
  },
  "spawn": {
    "enforceStandards": true,
    "_notes": "spawn-standards.sh (PreToolUse Agent|Task, first in chain) blocks teammate spawns that violate the skill's conventions: names must be <dreamname>-<task-slug> from the dream roster, and typed personas (luna/morpheus/lucid/nebula/oracle) must spawn with subagent_type dreamteam:<name>. Escape per-spawn: STANDARDS-EXEMPT: <reason> in the prompt. Added 2026-07-01 after coordinators kept drifting from the naming/type conventions."
  },
  "worktree": {
    "enforce": true,
    "_notes": "worktree-guard.sh (PreToolUse Edit|Write in TEAMMATE sessions) blocks writes outside an agent's assigned worktree (its team-config cwd) when that cwd is under .claude/worktrees/. Shared-checkout spawns (cwd not a worktree) are unaffected. Escape: orchestrator sets enforce=false, or the agent escalates via SendMessage. Added 2026-07-01 \u2014 converts the skill's #1 failure mode (2 collision incidents) from prompt discipline to harness law."
  },
  "scope": {
    "memoryHigh": "20G",
    "memoryMax": "24G",
    "memorySwapMax": "8G",
    "autoAttach": true,
    "_notes": "Caps for BOTH containment paths: launch-dreamteam.sh (whole-session scope) and scope-attach.sh (autoAttach=true: every spawn attaches THIS PROJECT'S live agent procs into dreamteam-<project>.scope via AttachProcessesToUnit \u2014 per-project since #19, so scope==project for fleet attribution and one project's runaway can't throttle another's fleet; before that, one shared dreamteam-agents.scope held everyone, added after the 2026-07-01 16:06 oomd kill). Name precedence: DREAMTEAM_SCOPE_NAME env > scope.name (add here to pin) > dreamteam-<repo basename, sanitized>. Caps apply PER SCOPE \u2014 N projects can nominally reserve N*MemoryHigh; the host RED-tier floor and systemd-oomd remain the global guards. MemoryHigh = throttle; MemoryMax = hard kill inside the agents' cgroup (orchestrators stay outside and survive); MemorySwapMax bounds the team's swap. Size to leave the GUI/host a few GB."
  },
  "local": {
    "enabled": false,
    "host": "http://localhost:11434",
    "model": "qwen2.5:14b-instruct-q4_K_M",
    "embedModel": "nomic-embed-text:v1.5",
    "timeoutSec": 120,
    "_notes": {
      "enabled": "The OPTIONAL local-model (ollama) lane — issue #16. DEFAULT false = the whole lane is a clean no-op and NOTHING in the default path touches ollama. scripts/local-model.sh reads this ==false-safe: only a literal `true` arms it (absent/null/false all stay off), sidestepping the jq `// true` trap that once made speech.enabled un-disableable. This is NOT a third agent-brain tier — JP's 2026-07-01 ruling stands (agent reasoning = Fable 5 → latest Opus; no sonnet/haiku/local). The lane is ONLY for work that doesn't need frontier reasoning: mechanical bulk, cron summaries (morning-briefing/self-audit), embeddings, and pre-filter triage. Anything it generates that lands in a repo goes through the oracle (Fable/Opus) before commit. Env DREAMTEAM_LOCAL_ENABLED overrides per-invocation.",
      "model": "Default generation model. qwen2.5:14b-instruct-q4_K_M (~9 GB, fits the 11 GiB 2080 Ti) is on-disk here and issue-endorsed. The lane NEVER auto-pulls — a missing model just fails the call (exit 3 → caller falls back to cloud). For a dedicated coder model, `ollama pull qwen2.5-coder:7b` and repoint this. Env DREAMTEAM_LOCAL_MODEL / --model override.",
      "embedModel": "Embeddings model for the --embed path (palace/dreamteam search). nomic-embed-text:v1.5 (768-dim) is on-disk. Env DREAMTEAM_LOCAL_EMBED_MODEL override.",
      "host": "ollama HTTP API base URL. Env DREAMTEAM_LOCAL_HOST override.",
      "timeoutSec": "Per-request curl cap in seconds (a cold model load can take tens of seconds on the first call). Env DREAMTEAM_LOCAL_TIMEOUT override.",
      "memoryAccounting": "OPEN (issue #16 bullet 3, out of THIS change's scope): a resident ollama model is ~7-9 GB of RSS/VRAM that mem-gate CANNOT see (it counts only claude procs). Dormant while enabled=false. Before arming the lane on a box that also runs fleets, bump memory.hostReserveMB to cover the resident model, or attach the ollama server to the containment scope — else admission math under-counts by a whole model."
    }
  }
}